name: randomizer_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0 # For state management
  url_launcher: ^6.3.0 # To handle URL links
  cached_network_image: ^3.4.0 # To handle images
  hive: ^2.2.0
  hive_flutter: ^1.1.0
  path_provider: ^2.1.4  # Required to get the correct paths for storing Hive boxes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.0
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
